<%= form_tag(search_path, method: :get) do %>
    <div class="form-group">
      <%= label_tag :keywords, '何食べた？' %>
      <%= text_field_tag :keywords, keywords, class: 'form-control' %>
    </div>

    <%= submit_tag '送信', class: 'btn btn-primary' %>
<% end %>
