<%= form_for(kondate_history) do |f| %>
  <% if kondate_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kondate_history.errors.count, "error") %> prohibited this kondate_history from being saved:</h2>

      <ul>
      <% kondate_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
